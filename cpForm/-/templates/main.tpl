<div class="{__NODE_ID__}" instance="{__INSTANCE__}">

    <!-- field -->
    <div class="field {CLASS} {LEVEL_CLASS}">
        <div class="label">{LABEL}</div>
        <div class="controls">
            <!-- field/control -->
            <div class="control">{CONTENT}</div>
            <!-- / -->
        </div>
    </div>
    <!-- / -->

</div>
